<input
x-ref="input-{{ $name }}"
name="{{ $name }}"
value="{{ old($name, $value) }}"
id="{{ $name }}"
class="input h-10 shadow-sm appearance-none border w-full py-2 px-3 text-slate-700 leading-tight focus:outline-none rounded-md border-slate-300"
/>
