<textarea x-ref="input-{{ $name }}" name="{{ $name }}" id="{{ $name }}" rows="{{ $rows }}"
    class="input shadow-sm appearance-none border w-full py-2 px-3 text-slate-700 leading-tight focus:outline-none rounded-md border-slate-300">{{ $slot }}
</textarea>
